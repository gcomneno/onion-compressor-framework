[pytest]
markers =
    p0: Core invariants (must never break)
    p1: Robustness / edge cases
    p2: Performance / torture / slow
    slow: Slow tests
    prop: Property-based tests
    fuzz: Fuzz tests
    bench: Benchmarks
